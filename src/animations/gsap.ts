/**
 * RenewableIQ GSAP Core
 * Prepares GSAP + ScrollTrigger registration for future pinned scroll storytelling.
 */

import gsap from 'gsap';
import { useGSAP } from '@gsap/react';

export { gsap, useGSAP };
